[pytest]
testpaths = tests
addopts = -ra -q
asyncio_mode = auto
markers =
    unit: pure unit tests (no media/ML required)
    integration: tests requiring FFmpeg and the full stack
    ml: tests requiring the SigLIP model download
    asyncio: async tests (pytest-asyncio)
filterwarnings =
    ignore::DeprecationWarning
